services:
  github-runner:
    image: juliansantosinfo/github-runner:${RUNNER_VERSION:-latest}
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
